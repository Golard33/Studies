#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;
/* retourne un nombre compris entre 0 et maxi-1 */
int nombreAlea(int maxi) {
	srand(time(NULL));
	return(rand() % maxi);
}
int main() {
	NbMyst = rand(10);
	NbMyst = nombreAlea(10);
}
